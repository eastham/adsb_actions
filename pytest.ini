[pytest]
log_cli = True
log_cli_level = DEBUG
log_format = %(levelname)-8s %(module)s:%(lineno)d: %(message)s
markers =
    slow: end-to-end tests that run the real pipeline on real ADS-B data (seconds each)
pythonpath =
    src
    tests
