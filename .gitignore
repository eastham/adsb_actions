# Project-specific
private.yaml

# Auto-generated airport configs
examples/generated*
src/tools/.cache/


# venv
.venv
.env
pyvenv.cfg
python3

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# C extensions
*.so

# Distribution / packaging
bin/
build/
develop-eggs/
dist/
eggs/
/lib/
lib64/
logs/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg
.DS_Store

# Data analysis output files
*.log
*.csv
*.out
*.gz

# Analysis-generated HTML and maps
src/tools/analysis/*.html
src/tools/analysis/*.png
src/adsb_actions/*.html

# ML model files
*.pth
*.h5
*.pkl
*.joblib

# Analysis directories
remote/
traces/
acas/
heatmap/
/tiles*
/data*
/output/
local/

# local test sandbox for the v2 CLI (copies of cell data; never commit)
.test_data/

# v2 pipeline test fixtures are real ADS-B shards (.gz) — keep them despite the
# global *.gz ignore above, so the end-to-end pipeline tests can run from a clone.
!tests/fixtures/v2_pipeline/
!tests/fixtures/v2_pipeline/**

# notes
COMMANDS

# Cloudflare worker local dev cache
examples/airbornehotspots/cloudflare-worker/.wrangler/

# text files (tracked examples/*.txt files are exempt by virtue of already being in the index)
*.txt
!airport_lists/*.txt
